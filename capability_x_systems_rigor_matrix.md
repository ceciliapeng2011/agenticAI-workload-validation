# 评测体系二维坐标图：能力金字塔 × 系统严谨度，六个 Runtime 在哪个格子里，τ-bench 方法论该嫁接到哪

> 
> 承接：`agentic_workload_research.md`（方法论笔记）、六份 `*_agentic_evaluation_investigate.md`（实测证据）、`cross_comparison_agentic_evaluation.md`（横向对比）、`agentic_test_design_proposal.md`（测试设计方案）
> 本文档做的事：把"MMLU→MT-Bench→Tool-Calling→SWE-bench"这条能力演进链，和六次调查发现的"系统严谨度普遍卡在很低的档位"这件事，画成一张二维图，再把六个系统实际摆进对应的格子，最后定位 τ-bench 方法论该嫁接在哪个坐标上。

## 一、两根轴分别是什么

### 横轴：能力金字塔（衡量"考的是什么能力"，越往右越难、越综合）

在原有四级之间插入 τ-bench 作为独立一级——它比单次工具调用格式检查更难（需要多轮、需要维持状态一致性），但比"真实修改一个代码仓库"更收敛（任务空间有限、终态可被精确定义），恰好卡在 Tool-Calling 和 SWE-bench 之间：

```
X1              X2            X3                  X4                    X5
MMLU/GSM8K  →  MT-Bench  →  Tool-Calling/JSON  →  τ-bench          →  SWE-bench
客观选择/计算    对话连贯性      单次工具调用格式       多轮状态一致的         开放式真实代码库修改
（背诵/推理）    （LLM-judge      /参数正确性           agentic 任务          （无预设终态，靠测试
                 主观打分）      （BFCL 式）           （终态匹配+pass^k）      套件跑分）
```

### 纵轴：系统严谨度（衡量"这个能力是在什么样的系统真实感下被验证的"，越往上越接近生产环境）

这根轴是六次调查报告实际在测的东西，原图完全没有画出来：

```
Y4  生产 trace 回放       真实 hash_ids/时间戳驱动的负载重放（Mooncake 式），常态化接入 CI 回归
Y3  agentic 感知 serving  多轮会话、前缀共享建模、依赖驱动到达，作为常态化 CI 的性能/精度回归
Y2  并发 serving 压测     数据集采样 + 并发 + 到达过程建模（ShareGPT + 泊松），常态化 CI
Y1  单请求微基准          单个/固定 prompt，无并发、无数据集、跑分靠手动比对
Y0  无系统维度             纯离线评测一个模型/agent 的能力，不涉及任何 serving 层参数
```

## 二、六个系统 + τ-bench 在这张图上的实际坐标

先给结论，再逐格解释：**六个系统的散点几乎全部贴着坐标轴走——要么沿着横轴爬（能力测得挺全但系统严谨度低），要么在纵轴上够到了一定高度但横轴上卡在很低的能力级别（OpenVINO GenAI）。没有一个点落在右上象限（X4/X5 × Y3/Y4）。**

```
Y（系统严谨度）
 ↑
Y4 │
   │
Y3 │                                  · SGLang(GSP结构校验，功能测试非性能追踪)
   │
Y2 │  · vLLM(bench_serving)    · SGLang(bench_serving)    · TRT-LLM(base_perf_*.csv)
   │
Y1 │  · llama.cpp(llama-bench)  · Ollama(cmd/bench)  · OpenVINO(llm_bench)
   │           ▲
   │           │ 但 OpenVINO 在 X1.5 位置纵向够到了 Y2.5（性能阈值+精度阈值联合断言）
Y0 │──────────────────────────────────────────────────────────· τ-bench(原生，agent层评测，不测serving)
   └────────────────────────────────────────────────────────────────────────→
     X1            X2         X3                    X4                X5
   MMLU/GSM8K   MT-Bench   Tool-Calling/JSON       τ-bench           SWE-bench
```

（这张图是示意，不是精确坐标——目的是让"没人在右上"这件事一眼看出来。）

### 逐系统定位

| 系统 | 横轴：实际测到哪一级 | 纵轴：实际测到哪一档 | 关键限制 |
|---|---|---|---|
| **vLLM** | X1（lm-eval，38 配置）+ X3（`tests/tool_use/`，但 fixture 伪多轮，11/12 关闭前缀缓存） | Y2（`bench_serving.py`，ShareGPT+泊松）；rust-bench 具备 Y3 能力但未接入 CI | X3 的测试深度不够（伪多轮），Y3 能力存在但不算"达成"（未被日常验证） |
| **SGLang** | X1（4 配置）+ X3（detector 单测 + 真两轮实现但是死代码） | Y2 常态化；`test_gsp_multi_turn` 摸到 Y3，但只验证"结构对不对"（日志级前缀增长），不产出性能数字，且 Mooncake（Y4 潜力）未被日常验证 | X3 的真两轮能力"写对了没跑"；Y3 是功能测试不是性能/精度回归 |
| **TensorRT-LLM** | X1（224 测试，含 reuse×量化 A/B）+ X3（Harmony 真两轮，六者最深）+ X5（`examples/scaffolding` 真实 SWE-bench Coder，但脱离 CI） | Y2 常态化（`base_perf_pytorch.csv`） | 唯一横轴摸到 X5 的系统，但 X5 完全脱离 Y 轴（纯手动，无 CI 追踪）；X1 上的"reuse 当精度变量"是六者最扎实的联合测试，但发生在能力金字塔最底层，没有延伸到 X3/X4 |
| **llama.cpp** | X1（仅困惑度/KL 散度，比任务型评测更弱）+ X3（`test_calc_result`，唯一断言具体数值的工具调用测试） | Y1（`llama-bench`）；官方 Y2 级 CI（`bench.yml`）已停用两年 | X3 最有价值的测试标记 `slow`，被移出日常门禁；纵轴实质上是负值（曾有更高档位后退化到零） |
| **Ollama** | X1（无，完全委托上游）+ X3（21 模型矩阵，广度最大，但仅单轮，未接入公开 CI） | Y1（`cmd/bench`，单请求） | 横轴 X3 广度全场最大，深度和系统严谨度全场最低；`agent/compactor.go` 是纵轴之外的第三维度（会话状态管理），本图暂未纳入 |
| **OpenVINO GenAI** | 介于 X1-X2 之间（LongBench 长文本 QA/摘要/分类，比纯客观题综合，但仍是单轮）+ X3（`VLLMParserWrapper` 单测 + `test_react_sample_refs` 但判定目标错位，不算真正测到 X3） | **Y2.5**（`test_kv_cache_eviction`：精度阈值+压缩比阈值同断言，六者纵轴严谨度最高） | 纵轴上够得最高，但发生的横轴位置最低——最严谨的方法论用在了离 agentic 核心能力最远的任务上 |
| **τ-bench（外部方法论，非六系统之一）** | **X4**（原生定义在这一级：多轮、双向状态控制、终态匹配、pass^k） | **Y0**（原生完全不涉及 serving 层——它评估的是"agent+model"整体，不关心背后跑在什么 Runtime 配置下） | 这是关键：τ-bench 本身没有系统维度，它是一套**可移植的方法论**，不是一个可以直接摆进纵轴的系统 |

## 三、τ-bench 方法论具体该怎么"嫁接"——它不该被理解成图上的一个点，而是一套可以搬到任意坐标的"评分引擎"

τ-bench 真正有价值、值得抽出来复用的不是它的具体任务（retail/airline/telecom domain），而是两个可移植的方法论组件：

1. **终态匹配（state matching）**：判定标准是"数据库/系统最终状态是否等于标注好的目标状态"，不是"这句话像不像人话"（文本相似度）或"格式合不合法"（JSON schema 校验）
2. **pass^k 可靠性指标**：同一任务重复跑 k 次，全部成功才算过，用来量化"这个能力在统计意义上稳不稳"，而不是"跑一次刚好通过"

这两个组件的价值在于：它们和"任务内容是什么"无关，可以被**移植到本图纵轴的任何一个档位、横轴的 X3 位置上**，用来把"系统层是否真正保住了 agentic 能力"这件事测得更严谨——这正是六次调查里普遍缺失的一环。具体嫁接方式：

```
把 τ-bench 的终态匹配 + pass^k，
搬到 X3（Tool-Calling）这一级，
在 Y2/Y3（并发 serving / agentic 感知 serving）的配置矩阵下跑
                    ↓
        得到图上目前完全空白的坐标：(X3.5, Y2.5)
        —— "工具调用任务的终态是否正确，
            在前缀缓存/KV量化/投机解码组合开启时，
            以 pass^k 的可靠性标准重复验证"
```

这正是 `agentic_test_design_proposal.md` 里那份测试设计方案在做的事——它本质上就是"把 τ-bench 的评分方法论，嫁接到 Tool-Calling 这一级、并要求在多组系统优化配置下都成立"。可以说：**这份图上标出的空白坐标，就是那份测试设计方案试图去填的地方。**

## 四、纵轴的一处修正：系统严谨度不是单一标量，要按负载形状分别打分

在追问"极端 schema 压测哪家有"时发现一个案例，逼着这张图的纵轴要做一次修正。

实测结果：六个系统里只有 vLLM 沾边，而且是两个互不相干的碎片——`tests/v1/structured_output/test_regex_compilation_timeout.py`（对应真实安全公告 GHSA-rwxx-mrjm-wc2m 的 ReDoS 防护测试，确认在常规 CI 里跑，但断言的是"超时包装器逻辑对不对"，背后用 mock 顶替了真实 xgrammar 编译器）和 `benchmarks/benchmark_serving_structured_output.py`（真实并发 serving 压测，但代码里 `dataset.filter(lambda item: not has_xgrammar_unsupported_json_features(schema))` **主动过滤掉了复杂/不支持的 schema**）。两者都存在，却从未被拼在一起过；另外五家在这个方向上是零命中。

这个案例说明：**纵轴的"系统严谨度"其实是按负载形状分别计分的，不是一个系统只有一个纵坐标**。vLLM 那个压测脚本，在"正常 schema 吞吐"这个负载形状下够得到 Y2，但在"对抗/极端 schema 鲁棒性"这个负载形状下是 Y0——同一个系统、同一段代码附近，两个数完全脱钩。原图纵轴的五档定义（Y0~Y4）默认隐含"负载都是正常请求"，需要显式加一个正交提示：

```
                          正常负载严谨度（原纵轴 Y0~Y4）
                                  ↑
                    Y2 │  vLLM benchmark_serving_structured_output.py
                       │
                    Y0 │──────────────────────────────→ 对抗/极端负载严谨度（同一系统需要单独打分的一维）
                                  Y0        （六个系统在这根轴上全部挂零，vLLM 稍好但两个碎片没拼起来）
```

对 agentic 场景的直接意义：工具 schema 在多 Agent/MCP 场景里可能来自不完全可信的第三方（一个恶意或写坏的 MCP server 可能返回巨型枚举、灾难性回溯正则），这属于**可用性风险**，和"精度"是两个维度——一个系统可以精度很高、正常负载吞吐很好，同时在恶意输入下被轻易拖垮。目前六家里只有 vLLM 表现出"意识到了这是问题"（有安全公告驱动的修复），但也没人把这份意识延伸成一条完整的、负载级别的验证链路。

## 五、这张图揭示的核心结论

1. **横轴上走得最远的系统（TRT-LLM，摸到 X5/SWE-bench），纵轴摸到最低**——`examples/scaffolding` 里的 SWE-bench Coder 完全不受 CI 约束，能力越靠右，越容易变成"纯粹的能力展示，脱离系统级验证"。这印证了此前报告里的判断：能力金字塔爬得越高，系统层面的把关反而越松。

2. **纵轴上走得最远的系统（OpenVINO GenAI），横轴停得最早**——`test_kv_cache_eviction` 的联合断言方法论是六者最严谨的，但用在了长文本 QA 这个相对基础的能力级别上，没有勇气/资源把同样的方法论用在 Tool-Calling 或更高的能力级别上。这说明"系统严谨度"和"能力复杂度"目前在工程实践里是互相竞争资源的两件事，没有谁把两者都做到位。

3. **τ-bench 提醒我们：能力评测和系统评测的"严谨度"是两套完全独立的方法论，可以互相借用**——τ-bench 解决的是"如何客观判定一个 agent 任务做对了没有"，这套方法论本身不带系统视角，但恰恰因为它不带系统视角，才可以被无缝嫁接到任何一个 Runtime 的任意配置矩阵下，去补上纵轴。它是这张图上唯一一个"轻量到可以随意搬动"的方法论组件。

4. **空白的右上象限，就是行业下一步真正的机会所在**——不是"再发明一个更难的能力评测"（横轴已经推到 SWE-bench 这么远了），也不是"再发明一个更大规模的 serving 压测"（纵轴的 Mooncake trace 已经证明生产级 trace 回放是可行的），而是**把这两件已经分别被证明可行的事情，第一次真正拼在一起，作为日常 CI 的一部分去跑**。
